<Page x:Class="InjectionSoftware.InjectionPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
      xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
      xmlns:local="clr-namespace:InjectionSoftware"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="InjectionPage" Background="#FFEBEBEB">

    <Grid>        
    <ItemsControl ItemsSource="{Binding Injections}">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <!-- Template for each injection case-->
                <Grid>
                    <!-- outline of the injection case-->
                    <Border CornerRadius="4"
                            Margin="3"
                            Background="White">
                        <Border.Effect>
                            <DropShadowEffect ShadowDepth="2" Color="#FF666666" BlurRadius="3" Opacity="0.7" />
                        </Border.Effect>
                    </Border>
                    
                    <!-- actual content of the case-->
                    <DockPanel Margin="3">
                        <!-- Injection case number-->
                        <Border BorderBrush="Black"
                                BorderThickness="2"
                                CornerRadius="4"
                                Padding="4"
                                Margin="5"
                                HorizontalAlignment="Left"
                                Width="{Binding ActualHeight, RelativeSource={RelativeSource Self}}">
                            <Viewbox Stretch="Uniform">
                                <TextBlock HorizontalAlignment="Center" 
                                           VerticalAlignment="Center"
                                           Text="{Binding CaseNumber}"
                                           Style="{DynamicResource MahApps.Styles.TextBlock.HamburgerMenuHeader}">
                                </TextBlock>
                            </Viewbox>
                        </Border>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition MaxWidth="38" Width="*"/>
                                    <ColumnDefinition MaxWidth="38" Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Assigned doctor -->
                                <Grid Grid.Column="3">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="2*"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <!-- the actual banner -->
                                    <Grid Margin="5 0 5 0" Background="#FF5A95FF" >
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>

                                        <!-- the doctor icon -->
                                        <iconPacks:PackIconUnicons Kind="UserMd" HorizontalAlignment="Center" Margin="2" Foreground="White"/>

                                    </Grid>
                                </Grid>

                                <!-- FDG/ PSMA -->
                                <Grid Grid.Column="2">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="2*"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <!-- the actual banner -->
                                    <Grid Margin="5 0 5 0" Background="#FFAA6FFF" >
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>

                                        <!-- the doctor icon -->
                                        <TextBlock>
                                            c
                                        </TextBlock>

                                    </Grid>
                                </Grid>

                                <!-- Patient information -->
                                <Grid Grid.Column="0">
                                    <Viewbox Stretch="Uniform">
                                        <TextBlock Text="{Binding Patient.PatientFullname}" VerticalAlignment="Center" Margin="2" />
                                    </Viewbox>                                    
                                </Grid>
                            </Grid>


                        </DockPanel>
                </Grid>
            </DataTemplate>
        </ItemsControl.ItemTemplate>

        <!-- Grid declartion-->
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                </Grid>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>

        <!-- Grid row, coloumn location declaration-->
        <ItemsControl.ItemContainerStyle>
            <Style>
                <Setter Property="Grid.Row" Value="{Binding Row}"/>
                <Setter Property="Grid.Column" Value="{Binding Column}"/>
            </Style>
        </ItemsControl.ItemContainerStyle>

    </ItemsControl>
    </Grid>
</Page>
